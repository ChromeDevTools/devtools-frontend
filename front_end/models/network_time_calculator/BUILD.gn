# Copyright 2025 The Chromium Authors
# Use of this source code is governed by a BSD-style license that can be
# found in the LICENSE file.

import("../../../scripts/build/ninja/devtools_entrypoint.gni")
import("../../../scripts/build/ninja/devtools_module.gni")

devtools_module("network_time_calculator") {
  split_compilation = true
  sources = [
    "Calculator.ts",
    "NetworkTimeCalculator.ts",
    "RequestTimeRanges.ts",
  ]

  ts_deps = [
    "../../core/common:bundle",
    "../../core/i18n:bundle",
    "../../core/sdk:bundle",
    "../../generated",
  ]
}

devtools_entrypoint("bundle") {
  split_compilation = true
  entrypoint = "network_time_calculator.ts"

  ts_deps = [ ":network_time_calculator" ]

  visibility = [
    ":*",
    "../*",
    "../../panels/ai_assistance/*",
    "../../panels/network/*",
    "../../panels/profiler/*",
    "../../ui/legacy/components/perf_ui/*",
  ]
}
