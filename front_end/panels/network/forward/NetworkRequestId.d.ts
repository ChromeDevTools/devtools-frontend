import type * as SDK from '../../../core/sdk/sdk.js';
export declare class NetworkRequestId {
    requestId: string;
    manager: SDK.NetworkManager.NetworkManager;
    constructor(requestId: string, manager: SDK.NetworkManager.NetworkManager);
}
