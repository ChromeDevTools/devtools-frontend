/**
 * Returns `true` when `object` is a matcher
 *
 * @private
 * @param {unknown} object A value to examine
 * @returns {boolean} Returns `true` when `object` is a matcher
 */
declare function isMatcher(object: unknown): boolean;
export = isMatcher;
