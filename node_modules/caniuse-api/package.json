{
  "name": "caniuse-api",
  "version": "4.0.0",
  "description": "request the caniuse data to check browsers compatibilities",
  "repository": "https://github.com/MoOx/caniuse-api.git",
  "keywords": [
    "caniuse",
    "browserslist"
  ],
  "authors": [
    "nyalab",
    "MoOx"
  ],
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "dependencies": {
    "browserslist": "^4.0.0",
    "caniuse-lite": "^1.0.0"
  },
  "devDependencies": {
    "@types/caniuse-lite": "^1.0.5",
    "oxlint": "^1.68.0",
    "typescript": "^6.0.3"
  },
  "scripts": {
    "build": "tsc",
    "lint": "oxlint src",
    "prepublishOnly": "npm run build",
    "test": "npm run lint && node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON --test test/*.ts",
    "release": "npx npmpub"
  }
}
