import { isIdentifier, isReturnStatement } from './node-types.js';
function isExplicitUndefined(node) {
    return node !== undefined && isIdentifier(node) && node.name === 'undefined';
}
export function isReturnOfUndefined(node) {
    const { argument } = node;
    const isImplicitUndefined = argument === null;
    return isImplicitUndefined || isExplicitUndefined(argument);
}
export function findReturnStatement(nodes) {
    return nodes.find(isReturnStatement);
}
//# sourceMappingURL=return-statement.js.map